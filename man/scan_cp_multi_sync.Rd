% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_cp_multi_sync.R
\name{scan_cp_multi_sync}
\alias{scan_cp_multi_sync}
\title{Multivariate Synchronized Changepoint Detection}
\usage{
scan_cp_multi_sync(
  Y,
  w = 100,
  method = c("L1", "L2", "max"),
  mlp_params = list(),
  detector_params = list(),
  combine_params = list(),
  ecdf_params = list()
)
}
\arguments{
\item{Y}{Numeric matrix (n × p). Each column is a signal dimension.}

\item{w}{Integer. Window size for rolling MLPs and detector.}

\item{method}{Character. Combination method: "L1", "L2", or "max".}

\item{mlp_params}{List of parameters passed to \code{fit_mlp()}.}

\item{detector_params}{List of parameters passed to \code{calc_detector()}.}

\item{combine_params}{List of parameters passed to \code{combine_detectors()}.}

\item{ecdf_params}{List of parameters passed to \code{detect_cp_ecdf()}.}
}
\value{
A list containing:
\describe{
\item{detectors}{List of univariate detectors (one per dimension).}
\item{joint_detector}{Joint detector vector.}
\item{contributions}{Matrix of per-dimension contributions.}
\item{changepoints}{Detected synchronized changepoints.}
\item{cp_contributions}{Contribution of each dimension at each CP.}
\item{ecdf_output}{Full output of \code{detect_cp_ecdf()}.}
}
}
\description{
Implements the synchronized multivariate changepoint pipeline:
\enumerate{
\item Fit univariate MLP-based detectors for each dimension.
\item Combine detectors into a joint detector using L1/L2/max.
\item Detect changepoints on the joint detector.
\item Compute per-dimension contributions at each detected CP.
}
}
