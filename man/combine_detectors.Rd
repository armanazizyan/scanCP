% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_cp_multi_sync.R
\name{combine_detectors}
\alias{combine_detectors}
\title{Combine Multiple Detector Statistics into a Joint Detector}
\usage{
combine_detectors(
  det.lst,
  method = c("L1", "L2", "max"),
  ma_window = NULL,
  circular = FALSE,
  scale_contributions = TRUE,
  scale_joint = TRUE
)
}
\arguments{
\item{det.lst}{List of numeric detector vectors (all same length).}

\item{method}{One of \code{"L1"}, \code{"L2"}, \code{"max"}.}

\item{ma_window}{Optional integer. If provided, each detector is smoothed
using \code{ma()} with this window size.}

\item{circular}{Logical. Whether smoothing wraps around.}

\item{scale_contributions}{Logical. Whether to compute contribution weights.}

\item{scale_joint}{Logical. Whether to scale the joint detector to [0,1].}
}
\value{
A list containing:
\describe{
\item{joint}{Joint detector vector.}
\item{s.mat}{Scaled detector matrix.}
\item{det.mat}{Raw detector matrix.}
\item{contrib}{Contribution weights (or NULL).}
}
}
\description{
Combines several univariate detector vectors into a single joint detector
using L1, L2, or max aggregation. Optionally applies moving-average
smoothing and computes contribution weights.
}
